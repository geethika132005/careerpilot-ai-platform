import "dotenv/config";

// The user has the API key in local storage, but since we are running in Node,
// we just need to use a dummy key or see what models are publicly listed?
// Wait, we can't list models without a valid API key. We need the user's key?
// No, I don't have the user's API key. 
